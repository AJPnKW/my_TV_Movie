# File: requirements.txt
# Project: my_TV_Movie
# Version: v1.1.0 (2025-11-06)
# Purpose: Shared deps for GH Actions data build (and optional local use)

requests==2.32.3
python-dotenv==1.0.1

# Optional (recommended): progress + fast JSON
tqdm==4.66.5
orjson==3.10.7

# Flask is optional (local only); not required for GitHub Pages:
# Flask==3.0.3
