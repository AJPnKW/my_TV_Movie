# Auto detect text files and perform LF normalization
* text=auto

# Force LF for repo data formats (prevents LF<->CRLF churn on Windows)
*.json text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

data/data.json merge=ours
data/inputs_parsed.json merge=ours

