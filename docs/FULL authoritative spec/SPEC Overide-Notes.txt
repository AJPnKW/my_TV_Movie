

Copy/paste this into a new ChatGPT session when you want the system to generate the updated scripts, workflow, and Section 99 documentation.

# CHATGPT PROMPT (BEGIN)

You must override the existing SPEC with the following manual update.  
This prompt includes the design reasoning, constraints, workflow logic, schema philosophy, error-handling model, traceability rules, and interdependency chain that MUST be preserved.

Follow these rules strictly:

- Always return full, final, fully integrated files (scripts, YAML, SPEC sections).  
- Never give instructions, partial blocks, or placement notes.  
- Never invent numbering.  
- Match the structure I provide exactly.  
- No commentary, no summaries — only the requested outputs.  
- All code must be complete and runnable.  
- All SPEC sections must be complete and ready to paste.  
- Respect all reasoning and constraints below.

------------------------------------------------------------
DESIGN REASONING (MUST BE PRESERVED)
------------------------------------------------------------

1. TXT files are user-editable inputs.  
   They are intentionally simple, human-friendly, and error-prone.

2. JSON files must become the source of truth.  
   They provide structure, validation, normalization, and traceability.

3. A new parser script must:
   - Clean and normalize TXT input  
   - Enforce schema  
   - Normalize titles using TMDB official names  
   - Resolve missing IDs via TMDB search  
   - Maintain history of changes  
   - Use active flags instead of deletes  
   - Write atomically to avoid corruption  

4. fetch_tmdb.py must no longer parse TXT.  
   It must rely exclusively on JSON to ensure consistency.

5. The workflow must run in this order:
   parse_txt_to_json.py → fetch_tmdb.py → fetch_trakt.py → sync_trakt.py

6. Error handling must be deterministic and consistent across scripts:
   - Log malformed TXT lines  
   - Log TMDB failures  
   - Log ambiguous search results  
   - Append all errors to data.json["errors"]  
   - Retry TMDB API calls (3x, backoff 0.6s)  
   - Validate JSON schema before writing  

7. Traceability is mandatory:
   - No deletes  
   - active=false instead  
   - history[] entries for every change  
   - UTC timestamps  
   - Deterministic formatting  

8. Atomic writes are required:
   - Write to temp file  
   - Validate  
   - Replace original  

9. Interdependencies must remain intact:
   - parse_txt_to_json.py produces JSON  
   - fetch_tmdb.py consumes JSON  
   - fetch_trakt.py enriches data.json  
   - sync_trakt.py syncs external services  

10. TMDB normalization is required:
    - Always use official TMDB titles  
    - Prefer direct ID fetch  
    - Fallback to search when ID missing  

------------------------------------------------------------
JSON SCHEMA (REQUIRED)
------------------------------------------------------------

Each entry in shows.json/movies.json must be:

{
  "tmdb_id": int,
  "official_title": str,
  "user_title": str,
  "year": int | null,
  "seasons": "*" | [int],
  "active": bool,
  "history": [
    {
      "timestamp_utc": str,
      "action": "add"|"update",
      "changes": dict
    }
  ]
}

------------------------------------------------------------
CORRECT VS INCORRECT BEHAVIOR (MUST FOLLOW)
------------------------------------------------------------

### CORRECT:
- Full files returned with no missing sections  
- JSON schema exactly matched  
- History entries included on every change  
- TMDB official titles always used  
- Atomic writes implemented  
- Workflow steps updated in correct order  
- No deletes; inactive flag only  
- Deterministic formatting  

### INCORRECT:
- Returning only code fragments  
- Adding new fields not in schema  
- Renaming fields  
- Changing workflow order  
- Using TXT parsing inside fetch_tmdb.py  
- Omitting history entries  
- Writing JSON without atomic replacement  
- Inventing new numbering in SPEC  

------------------------------------------------------------
WORKFLOW PIPELINE UPDATE
------------------------------------------------------------

Update build-data.yml:

1. Run parse_txt_to_json.py FIRST  
2. Then run fetch_tmdb.py (updated to use JSON, not TXT)  
3. Then run fetch_trakt.py  
4. Then run sync_trakt.py  
5. Output final data/data.json  

------------------------------------------------------------
SCRIPT UPDATES REQUIRED
------------------------------------------------------------

A) NEW FILE: scripts/parse_txt_to_json.py  
Provide full working code.

B) UPDATED FILE: scripts/fetch_tmdb.py  
- Remove TXT parsing  
- Load from JSON  
- Fetch TMDB metadata  
- Produce data/data.json  
- Append errors to data.json["errors"]  
- Retry API calls (3x, backoff 0.6s)  
- Validate JSON schema  

C) UPDATED FILE: .github/workflows/build-data.yml  
- Add parse step before fetch_tmdb.py  
- Ensure consistent logging  
- Fail workflow on schema validation errors  

D) OPTIONAL MINOR UPDATE: fetch_trakt.py  
- Ensure compatibility with new data.json structure  

------------------------------------------------------------
SPEC DOCUMENTATION (SECTION 99)
------------------------------------------------------------

Generate a Section 99 entry documenting:
- Data Source Migration (TXT → JSON)  
- JSON Schema  
- Parser rules  
- Workflow pipeline changes  
- Error handling  
- Traceability (history, active flag)  
- Consistency rules  
- Interdependencies  
- Rationale  

------------------------------------------------------------
OUTPUT REQUIREMENTS
------------------------------------------------------------

Return the following FULL files:

1. scripts/parse_txt_to_json.py  
2. Updated scripts/fetch_tmdb.py  
3. Updated build-data.yml  
4. Section 99 SPEC entry  

No instructions.  
No summaries.  
Only complete, final, production-ready files.

# CHATGPT PROMPT (END)


# ⭐ **SECTION 99 — WIP / MANUAL OVERRIDES (FULL FILE)**  
*(Ready to paste into your SPEC repository)*

```markdown
/* =========================================================================================
[SECTION] 99 — Manual Overrides, WIP Changes, and Transitional Architecture Notes
[PROJECT] my_TV_Movie (My TV Hub)
[ROLE] Repository of in-progress design changes not yet merged into primary SPEC sections
[VERSION] v4.5.0
[UPDATED] 2025‑12‑20_00‑00‑00
[OWNER] Andrew & Brant (internal)

[NOTES]
- Section 99 stores authoritative overrides and transitional architecture updates.
- All entries here supersede earlier SPEC sections until formally merged.
- Once validated and stable, these entries will be integrated into Sections 1–7.
========================================================================================= */

# Section 99 — Manual Overrides and Transitional Updates

## 99.1 — Data Source Migration (TXT → JSON)

### Purpose
Migrate from TXT-based metadata ingestion to a JSON-based source of truth to improve data quality, normalization, traceability, and workflow reliability.

### Summary of Change
- `tv_list.txt` and `movies_list.txt` remain user-editable input files.
- A new script (`parse_txt_to_json.py`) converts TXT → JSON.
- JSON files (`shows.json`, `movies.json`) become the canonical source of truth.
- `fetch_tmdb.py` now reads JSON instead of TXT.
- Workflow updated to run parser before TMDB fetch.
- No deletes: entries removed from TXT are marked `active=false`.
- All changes tracked via per-entry `history[]`.

---

## 99.2 — JSON Schema (Authoritative)

Each entry in `data/shows.json` and `data/movies.json` must follow:

```json
{
  "tmdb_id": 12345,
  "official_title": "Official TMDB Title",
  "user_title": "User-entered title",
  "year": 2020,
  "seasons": "*" | [1, 2, 3],
  "active": true,
  "history": [
    {
      "timestamp_utc": "2025-12-20T00:00:00Z",
      "action": "add",
      "changes": {
        "field": "old→new"
      }
    }
  ]
}
```

### Schema Rules
- `tmdb_id` is required; search TMDB if missing.
- `official_title` must always match TMDB canonical naming.
- `user_title` preserves the user’s input.
- `seasons` supports `"*"`, lists, and ranges.
- `active=false` replaces deletion.
- `history[]` must record all adds/updates with UTC timestamps.
- JSON must be written atomically.

---

## 99.3 — Parser Rules (parse_txt_to_json.py)

### Responsibilities
- Read TXT files.
- Split lines by `|`.
- Extract: title, year, TMDB ID, seasons.
- Normalize whitespace and casing.
- Resolve missing TMDB IDs via search.
- Fetch official TMDB titles.
- Convert seasons (`*`, lists, ranges).
- Update JSON atomically:
  - Add new entries.
  - Update changed fields.
  - Mark removed entries as inactive.
- Append history entries for all changes.
- Validate JSON schema before writing.

### Error Handling
- Log malformed TXT lines.
- Log TMDB API failures.
- Log ambiguous search results.
- Log duplicates.
- Append all errors to `data.json["errors"]`.

---

## 99.4 — Workflow Pipeline Update

### New Required Order
1. `parse_txt_to_json.py`  
2. `fetch_tmdb.py`  
3. `fetch_trakt.py`  
4. `sync_trakt.py`  
5. Output: `data/data.json`

### Workflow Requirements
- Fail on schema validation errors.
- Use consistent logging across all scripts.
- All scripts must be deterministic.

---

## 99.5 — TMDB Normalization Rules

### Required Behavior
- Always prefer direct ID fetch (`/tv/{id}`, `/movie/{id}`).
- If ID missing, use TMDB search.
- Normalize titles to TMDB official names.
- Retry API calls 3× with exponential backoff (0.6s base).
- Log all failures.

---

## 99.6 — Traceability and Auditing

### Requirements
- No deletes; inactive flag only.
- All changes recorded in `history[]`.
- Timestamps must be UTC ISO8601.
- JSON must be deterministic and stable across runs.
- Atomic writes required for all JSON outputs.

---

## 99.7 — Interdependency Notes

### Required Chain
- Parser produces JSON.
- TMDB fetch consumes JSON.
- Trakt fetch enriches `data.json`.
- Sync script updates external services.

### Constraints
- No script may re-parse TXT except the parser.
- No script may mutate JSON except the parser.
- No script may bypass schema validation.

---

## 99.8 — Rationale

### Why TXT → JSON Migration
- TXT is human-friendly but structurally unreliable.
- JSON provides:
  - validation  
  - normalization  
  - traceability  
  - atomic updates  
  - consistent schema  
  - reliable downstream processing  

### Benefits
- Eliminates parsing errors.
- Ensures TMDB canonical naming.
- Adds full audit history.
- Improves workflow reliability.
- Enables future automation.

---

## 99.9 — Status
This override is **active** and must be treated as authoritative until merged into Sections 1–7.
