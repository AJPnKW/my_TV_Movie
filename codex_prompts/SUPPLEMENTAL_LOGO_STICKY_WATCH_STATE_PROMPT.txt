You are working in:

C:\Users\andrew\PROJECTS\GitHub\my_TV_Movie

Run with:
codex -m gpt-5.5 --dangerously-bypass-approvals-and-sandbox

SUPPLEMENTAL TASK:
Stop current broad work after the current safe checkpoint, then perform a targeted compliance pass using the updated documentation supplement.

Read first:
- docs/README.md
- docs/DOCUMENTATION_STANDARD.md
- docs/UI_COMPONENTS.md
- docs/ARCHITECTURE.md
- docs/supplements/LOGO_STICKY_WATCH_STATE_SUPPLEMENT.md

Do not redesign from memory. Implement the documented contract.

Current screenshot gaps to fix:
1. Logo renders huge/wide and with bad background. It must be small, aspect-ratio preserved, transparent/corrected, and fit within compact sticky header height.
2. Sticky dashboard section headers are not sticky. Implement reusable sticky section headers.
3. Icons still need rendered QA for fit/order/boxes. Confirm final row is: 🍿 ⌚ 🎫 💕 76%.
4. Watch Me should be deprecated as standalone primary nav unless documented with a unique purpose.
5. Add/manage route/view for watch_list, watched_status, favourite, Trakt mapping status, unmatched IDs, and sync queue.
6. Ensure docs and code agree.

Implementation rules:
- Prefer editing existing files.
- Do not add more overlay patch systems.
- Do not create duplicate docs.
- Do not create one-off scripts unless wired into the existing validator.
- Preserve Inputs Editor.
- Preserve existing routes with compatibility if deprecating Watch Me.
- Stage only intentional files.

Specific implementation checks:
- Header logo must use assets/custom/the_boys_hub_logo2.png or a documented corrected transparent runtime derivative.
- Logo CSS must use object-fit: contain, width:auto, max-height/height clamp, no stretching.
- Primary nav should not waste vertical space.
- If Watch Me is deprecated, remove it from primary nav but keep /web/watch_me.html compatibility.
- Add/manage watch state view reachable from Config or nav only if appropriate.
- Manage view must expose local-first watched_status/watch_list/favourite controls and Trakt scaffold/status.
- Sticky section header must account for sticky top nav offset.
- Add validator checks for these requirements.

Validation:
Run:
powershell -ExecutionPolicy Bypass -File scripts/validate_runtime.ps1

Run local server:
python -m http.server 8000

Check:
- /web/index.html
- /web/calendar.html
- /web/shows.html
- /web/movies.html
- /web/watch_me.html
- /web/discover.html
- /web/config.html
- /web/inputs_editor.html

Return:
1. git status
2. commit hash
3. files changed
4. screenshot gaps fixed
5. manage watch-state status
6. Watch Me decision
7. validation results
8. local URLs
